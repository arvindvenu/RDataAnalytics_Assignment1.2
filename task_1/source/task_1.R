v <- c( 2,5.5,6)
t <- c(8, 3, 4)

# %/% would perform integer division. That is it will return the integer quotient 
# when left operand will be divided by the right operand

# so output would be 0 1 1
print(v%/%t)